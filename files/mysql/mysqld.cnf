# ANSIBLE MANAGED

[mysqld]
# Default character encodings for Confluence and JIRA
character-set-server=utf8
collation-server=utf8_bin

# Ignore directories called 'lost+found', prevents issue CLNTTHREE-422 happening again
ignore-db-dir=lost+found
