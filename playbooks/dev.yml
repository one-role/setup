---
# Playbook : dev.yml
# Author   : Ton Kersten
# Date     : 03-01-2019
#
# System wide playbook for the 'dev' environment

# Set all dynamic groups
- import_playbook: pre.yml
  tags: always

- name: run all playbooks, roles and tasks for 'dev'
  hosts: ansiblemanaged_True:&dev
  user: ansible
  become: True

  tasks:
    - name: dev | include "common" tasks
      import_tasks: tasks/common.yml
      tags: common

    - name: dev | include "ssh" tasks
      import_tasks: tasks/ssh.yml
      tags: [ ssh, openssh ]

    - name: dev | include "master" tasks
      import_tasks: tasks/master.yml
      when: "'master' in group_names"
      tags: master

    - name: dev | include "mysql" tasks
      import_tasks: tasks/mysql.yml
      when: "'mysql' in group_names"
      tags: mysql

    - name: dev | include "postgresql" tasks
      import_tasks: tasks/postgresql.yml
      when: "'pgsql' in group_names"
      tags: [ pgsql, postgresql ]

    - name: dev | include "nagios" tasks
      import_tasks: tasks/nagios.yml
      when: "'monitoring' in group_names"
      tags: monitoring

    - name: dev | include "builder" tasks
      import_tasks: tasks/builder.yml
      when: "'builder' in group_names"
      tags: builder

    - name: dev | include "elk" tasks
      import_tasks: tasks/elk.yml
      when: "'elk' in group_names"
      tags: elk

    - name: dev | include "proxy" tasks
      import_tasks: tasks/proxy.yml
      when: "'proxy' in group_names"
      tags: [ proxy, dhcp, openldap_proxy, squid ]

    - name: dev | include "rocketchat" tasks
      import_tasks: tasks/rocketchat.yml
      when: "'rocketchat' in group_names"
      tags: rocketchat

    - name: dev | include "gitlab" tasks
      import_tasks: tasks/gitlab.yml
      when: "'gitlab' in group_names"
      tags: gitlab

    - name: dev | include "wiki" tasks
      import_tasks: tasks/wiki.yml
      when: "'wiki' in group_names"
      tags: wiki

    - name: dev | include "jira" tasks
      import_tasks: tasks/jira.yml
      when: "'jira' in group_names"
      tags: jira

    - name: dev | include "backupclient" tasks
      import_tasks: tasks/backupclient.yml
      tags: [ common, backup, backupclient ]

- name: dev | show all hosts in environment 'dev' not managed by Ansible
  hosts: ansiblemanaged_False:&dev
  user: ansible
  become: True

  tasks:
    # Show all hosts not managed by Ansible
    - name: dev | include "managed hosts" tasks
      import_tasks: tasks/managed.yml

